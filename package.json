{
  "scripts": {
    "release:version": "node -p \"require('./addon/package.json').version\"",
    "release:test": "rm -rf addon/dist && rm -rf logs && mkdir logs && cd addon && rollup --config && cd ../test-app && JOBS=1 CI=1 ember test ci --silent --r xunit | tee ../logs/xunit.xml",
    "release": "cd addon && npm publish"
  },
  "private": true,
  "workspaces": [
    "addon",
    "test-app"
  ]
}
