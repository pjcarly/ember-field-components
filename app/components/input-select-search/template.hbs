<select class="selectpicker form-control" {{action 'valueChanged' on='change'}} tabindex="-1" data-live-search="true">
  {{#if emptyValue}}
    {{option-none selected=true}}
  {{/if}}
  {{#each selectOptions as |selectOption|}}
    {{option-selected selectOption value}}
  {{/each}}
</select>
