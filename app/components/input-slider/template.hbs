<Input
  id={{this.inputIdComputed}}
  class="form-control"
  disabled={{@disabled}}
  @type="range"
  @value={{this.computedValue}}
  @placeholder={{@placeholder}}
  @min={{this.min}}
  @max={{this.max}}
  @step={{this.step}} />