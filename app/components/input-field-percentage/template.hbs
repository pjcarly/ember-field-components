{{#if hasDecimals}}
  {{input-phone value=value valueChanged='valueChanged' placeholder=placeholder prefix=prefix suffix=suffix}}
{{else}}
  {{input-number value=value valueChanged='valueChanged' placeholder=placeholder prefix=prefix suffix=suffix}}
{{/if}}
