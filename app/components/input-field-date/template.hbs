{{#if isBootstrap}}
  {{input-date-bootstrap value=value valueChanged='valueChanged' placeholder=placeholder prefix=prefix suffix=suffix minDate=minDate maxDate=maxDate styleClass=styleClass showButton=showButton inputId=inputId}}
{{else}}
  {{input-date value=value valueChanged='valueChanged' placeholder=placeholder prefix=prefix suffix=suffix styleClass=styleClass inputId=inputId}}
{{/if}}
