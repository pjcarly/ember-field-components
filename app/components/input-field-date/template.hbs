{{#if isBootstrap}}
  {{input-date-bootstrap value=value valueChanged=(action 'valueChanged') placeholder=placeholder prefix=prefix suffix=suffix minDate=minDate maxDate=maxDate styleClass=styleClass showButton=showButton inputId=inputId widgetOptions=widgetOptions}}
{{else}}
  {{input-date value=value valueChanged=(action 'valueChanged') placeholder=placeholder prefix=prefix suffix=suffix styleClass=styleClass inputId=inputId}}
{{/if}}
