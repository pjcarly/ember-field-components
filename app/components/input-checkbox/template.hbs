<span class={{this.computedClass}}>
  {{#if @prefix}}
    <span class="input-group-text prefix">
      {{@prefix}}
    </span>
  {{/if}}
  <span class="form-check">
    <Input
      id={{@inputId}}
      @type="checkbox"
      @checked={{@value}}
      class="form-check-input"
      disabled={{@disabled}}
      placeholder={{@placeholder}}
      ...attributes
      {{on "input" this.toggleValue}}
    />
  </span>
  {{#if @suffix}}
    <span class="input-group-text suffix">
      {{@suffix}}
    </span>
  {{/if}}
</span>