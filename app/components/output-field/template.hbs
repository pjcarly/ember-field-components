{{#if isBlock}}
  {{field-label labelClass='fg-label' for=inputId model=model field=field}}
{{/if}}

{{#if hasBlock}}
  {{yield inputId}}
{{else if (component-exists componentName)}}
  {{component componentName model=model field=field placeholder=placeholder styleClass=styleClass prefix=prefix suffix=suffix showButton=showButton inputId=inputId inline=inline options=options}}
{{else}}
    Component {{componentName}} does not exists.
{{/if}}
