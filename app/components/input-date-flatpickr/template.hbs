<span class={{this.computedClass}}>
  {{#if @prefix}}
    <div class="input-group-prepend">
      <span class="input-group-text prefix">{{@prefix}}</span>
    </div>
  {{/if}}

  <EmberFlatpickr
    @id={{this.inputIdComputed}}
    @date={{readonly this.computedValue}}
    @flatpickrRef={{this.flatpickr}}
    @dateFormat={{this.flatpickrFormat}}
    @minDate={{this.minDate}}
    @maxDate={{this.maxDate}}
    @allowInput={{true}}
    @enableTime={{false}}
    @placeholder={{@placeholder}}
    @autocomplete="off"
    @class="form-control"
    @disabled={{@disabled}}
    @onChange={{action (mut this.computedValue)}} 
    @getFlatpickrRef={{action this.onFlatpickrInstance}}/>

  {{#if (or this.showButton @suffix)}}
    <div class="input-group-append">
      <button type="button" class="btn{{if this.options.btnClass (concat ' ' this.options.btnClass) ' btn-default'}}" {{on "click" this.toggleCalendar}}>
        <IconCalendar />
      </button>
      {{#if @suffix}}
        <span class="input-group-text suffix">{{@suffix}}</span>
      {{/if}}
    </div>
  {{/if}}
</span>