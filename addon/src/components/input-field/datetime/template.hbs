{{#if (or (eq this.widgetName "flatpickr") (eq this.widgetName "bootstrap"))}}
  <Input::DatetimeFlatpickr
    @value={{this.value}}
    @valueChanged={{this.setNewValue}}
    @preSetHook={{@preSetHook}}
    @placeholder={{@placeholder}}
    @prefix={{@prefix}}
    @suffix={{@suffix}}
    @class={{@inputClass}}
    @inputId={{@inputId}}
    @options={{this.inputOptions}}
  />
{{else}}
  <Input::Datetime
    @value={{this.value}}
    @valueChanged={{this.setNewValue}}
    @preSetHook={{@preSetHook}}
    @placeholder={{@placeholder}}
    @prefix={{@prefix}}
    @suffix={{@suffix}}
    @class={{@inputClass}}
    @inputId={{@inputId}}
    @options={{this.inputOptions}}
  />
{{/if}}