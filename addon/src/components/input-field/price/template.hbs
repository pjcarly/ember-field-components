<Input::Number
  @value={{this.value}}
  @valueChanged={{this.setNewValue}}
  @preSetHook={{@preSetHook}}
  @placeholder={{@placeholder}}
  @prefix={{@prefix}}
  @suffix={{this.currency}}
  @class={{@inputClass}}
  @inputId={{@inputId}}
/>